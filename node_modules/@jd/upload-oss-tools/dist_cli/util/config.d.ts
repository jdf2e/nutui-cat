import { uploadConfig } from "../model/uploadConfig";
declare class config {
    /**
     * 检查文件是否存在并生成
     * @private
     * @param {string} localFullPath 绝对路径
     * @returns {Promise<boolean>}
     * @memberof oss
     */
    static checkFile(localFullPath: string): Promise<boolean>;
    /**
     *
     * 保存文件
     * @param {uploadConfig} upload_config
     * @returns {Promise<boolean>}
     */
    static saveFile(package_config: uploadConfig): Promise<boolean>;
    /**
     * 读取文件
     */
    static readUploadConfigFile(): uploadConfig;
    /**
     * 初始化配置文件
     */
    static initUploadConfigFile(): uploadConfig;
}
export default config;
