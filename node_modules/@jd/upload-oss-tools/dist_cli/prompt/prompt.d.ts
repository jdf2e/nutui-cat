import { uploadTarget } from '../model/uploadTarget';
declare class prompt {
    private _jdoss_config;
    constructor();
    /**
     * 初始化应用，引导用户本次使用
     */
    initCommand(): Promise<void>;
    /**
     * 设置应用秘钥
     */
    settingKey(): Promise<unknown>;
    /**
     * 执行上传操作
     */
    upload(): Promise<void>;
    /**
     * 执行下载操作
     */
    download(): Promise<void>;
    /**
     *
     * 提示询问框
     * @param {string} msg
     * @returns {Promise<boolean>}
     * @memberof prompt
     */
    promptConfirm(msg: string): Promise<boolean>;
    /**
     * 管理本地上传队列
     *
     */
    settingQueue(): Promise<void>;
    /**
     *
     * 修改上传对象信息
     * @param {uploadTarget} _uploadTarget
     * @memberof prompt
     */
    actionUploadTarget(_uploadTarget: uploadTarget): Promise<void>;
    /**
     *
     * * 新增/修改 上传对象
     * @param {string} [type="modify|add"]
     * @param {uploadTarget} _uploadTarget
     * @returns {Promise<boolean>}
     */
    modifyUploadTarget(type?: string, _uploadTarget?: uploadTarget): Promise<boolean>;
}
export default prompt;
